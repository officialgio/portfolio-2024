import { Inner } from "../Inner/inner.component";

export const About = () => {
  return (
    <Inner>
      <div className="section">
        <h2>This is the the about page</h2>
      </div>
    </Inner>
  );
};
